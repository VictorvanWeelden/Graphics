using OpenTK;

namespace template
{
    public class Light
    {
        public Vector3 positie;
        public Vector3 kleur;
        public Light(Vector3 Pos, Vector3 Kleur)
        {
            positie = Pos;
            kleur = Kleur;
        }

    }
}
